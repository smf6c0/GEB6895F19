Example 5: Write pseudo code that will count all the even numbers up to a user defined stopping point.

userinput <- print(input: 'enter a number: ')

set i = 0
for i <=userinput:
if (even <= userinput %% 2 == 0)
  then
    print(even)
  else
    print('number is odd')
